% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{format_output}
\alias{format_output}
\title{Intelligent output formatting with smart truncation}
\usage{
format_output(obj, max_length = 200, preserve_structure = TRUE)
}
\arguments{
\item{obj}{The R object to format (any type)}

\item{max_length}{Maximum characters in output (default: 200)}

\item{preserve_structure}{Keep full structure for lists? (default: TRUE)}
}
\value{
Character string representation, never exceeds max_length + 3
}
\description{
Formats R objects for display with type-aware strategies to prevent
overwhelming output while preserving essential information.

Design Goals:
\itemize{
\item Show enough information to understand the object
\item Prevent screen overflow for large objects
\item Preserve structure for small objects
\item Handle all common R types gracefully
}
}
\details{
Formatting Strategy by Type:

\strong{NULL:}
Returns: "NULL"

\strong{Lists (small, ≤3 elements):}
Shows: Full structure
Example: list(a = 1, b = 2, c = 3)

\strong{Lists (large, >3 elements):}
Shows: list(length=100, first 3: ..., ...)

\strong{Vectors (≤10 elements):}
Shows: Full deparsed representation
Example: c(1, 2, 3, 4, 5)

\strong{Vectors (>10 elements):}
Shows: \verb{type[1:n] = first, second, third ... type[n] = last}
Example: \verb{integer[1:100] = 1, 2, 3 ... integer[100] = 100}

\strong{Matrices:}
Shows: \verb{matrix[rows x cols]: first_values ...}
Example: \verb{matrix[3x4]: 1 2 3 ...}

\strong{Data Frames:}
Shows: \verb{data.frame[rows x cols] columns: col1, col2, col3}
Example: \verb{data.frame[10 x 3] columns: x, y, z}
}
\section{Performance}{

Time: O(1) for most types (uses head/tail)
Space: O(k) where k = min(object_size, max_length)
}

\examples{
\dontrun{
# Small vector - full output
format_output(1:5)
# "1:5"

# Large vector - truncated
format_output(1:1000)
# "integer[1:1000] = 1, 2, 3 ... integer[1000] = 1000"

# Data frame - summary
format_output(mtcars)
# "data.frame[32 x 11] columns: mpg, cyl, disp, hp, ..."

# Matrix - dimensions
format_output(matrix(1:100, 10, 10))
# "matrix[10x10]: 1 2 3 ..."
}

}
\keyword{internal}
