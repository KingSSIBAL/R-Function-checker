# Decrypt a string

Decrypts a hex-encoded encrypted string.

## Usage

``` r
.cpp_decrypt(ciphertext, key = "")
```

## Arguments

- ciphertext:

  Hex-encoded encrypted string

- key:

  Optional encryption key (uses default if empty)

## Value

Decrypted plaintext
