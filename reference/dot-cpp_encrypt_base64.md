# Encrypt to base64

Encrypts a string and returns base64-encoded result.

## Usage

``` r
.cpp_encrypt_base64(plaintext, key = "")
```

## Arguments

- plaintext:

  Text to encrypt

- key:

  Optional encryption key

## Value

Base64-encoded encrypted string
